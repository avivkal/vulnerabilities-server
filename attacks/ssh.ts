import { SSH_PORT } from "../constants";

export const bruteForceSSH = (possibleAttacks: number[]): void => {
    // TODO - add if here
    // if it's possible, then push to array
    
    possibleAttacks.push(SSH_PORT);
}